import { StyleSheet, View } from "react-native";
import { SignUp } from "../../components";

const LoginScreen = () => {
	return <View>{true && <SignUp />}</View>;
};

export default LoginScreen;

const styles = StyleSheet.create({});
