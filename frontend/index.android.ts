import { AppRegistry } from "react-native";
import App from "./App";
/* 
if trying to import react-native-ble-manager/example from node_modules
*/
AppRegistry.registerComponent("closeAtHand", () => App);
